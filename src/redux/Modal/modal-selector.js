export const selectPrivacyModal = state => state.modal.isPrivacyModal;

export const selectTermsModal = state => state.modal.isTermsModal;

export const selectRegisterModal = state => state.modal.isRegisterModal;

export const selectSuccsess = state => state.modal.isSuccsess;

export const selectLoading = state => state.modal.isLoading;